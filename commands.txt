

LEFT off
- TO DO
- Create Faculty Accounts and set-up
- Set up search bar for courses on degree progress pages

- I created the faculty service. It broke the classSession and reset-db, so I will need to change those and the sample
data to reflect the changes made.
- I finished designing the basis of the degreeProgress page. Will need polished
- I created a test Project for software testing


- Consider adding a faculty portal where teachers can submit grades, pass students in their class, 
report grade percentage, and more. This could allow for calculating GPA, marking classes complete, 
Then removing classes from student's current courses

- I finished fixing ClassSessions. There is one error where course's reference to class session does not referesh
after deleting classSession. It does referesh after page reload.



NOTE TO SELF - Left off with trying to add user creation via application approval. Needs finished and debugged
- Add Pathfinding on all pages (start with admin pages). Basically the path to your current page appears:
/admin/students/{student_name} ||| Users can select 'students' to return to '/admin/students' or 'admin' to return to '/admin'


TO DO LIST
- DONE Complete New Student Application Form
- Web page pathfindings
- Student program selection
- Student Class Registration
- Student Support Tickets
- Student Update Account Details
- Student Transcript access 
- 

- Admin: Logs of edits made on students, courses, and degrees.
- Admin: Logs of edits made on accounts by students.
- Admin: Student Transcript creation 
- 

- Complete Website CSS
- Authentication + Authorization
- Server side validation on ALL forms
- Make EMAIL unique for draft applications and student accounts
- Email Services
- Set up PDF Creator | Student Applications | Student Summaries | Student Transcripts (CSU editions)
- Real DB connection and updates
- Deploy Website on Azure




REVIEW - 
Authentication + authorization
- Need builder.Services.AddCascadingAuthenticationState()
- Need builder.Services.AddAuthorization()
- Need builder.Services.AddIdentity<ApplicationUser, IdentityRole>()
    .AddEntityFrameworkStores<ApplicationDbContext>()
    .AddDefaultTokenProviders();
- Need builder.Services.ConfigureApplicationCookie(options => // Cookie configurations
{
    options.Cookie.Name = "StudentPortal.Identity";
    options.Cookie.HttpOnly = true;
    options.Cookie.SameSite = SameSiteMode.Lax;
    options.LoginPath = "/login";
    options.ExpireTimeSpan = TimeSpan.FromMinutes(30);
    options.SlidingExpiration = true;
});
- Need the app.UseAuthentication() + app.UseAuthorization()
- Login and logout use <form> NOT <EditForm>
- They must be done through HTTP requests through an app.map() method
- The submission happens from <form method="post" action="/account/login" >
- Make sure cookie is being created. See program.cs for details of route request and cookie creation.
- Roles are assigned at user creation.
- Cookie expiration, persistence, and other settings are set before app.build().

- I created relationships between User.MyProgram => current [ClassSessions], Registered [ClassSessions], [CompletedCourses]
With these relationships, I may need to add a new ClassSession.List<UserModels> RegisteredId and RegisteredUsers
for the modelBuilder to work properly. I am unsure what is best. I need to build more before deciding. Jotting
this note down to remind me this may cause a bug later (Right now current [classSessions] and Registered [ClassSessions]
point to the same navigation property)... SCRATCH THAT => I had to add it for modelBuilder to accept a migration


Setup authorization using ASP.NET's standard authorization core
dotnet add package Microsoft.AspNetCore.Components.Authorization

SEND GRID | Email service
dotnet add package SendGrid

450 character Lorem Text
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Praesent vitae justo nec erat feugiat tincidunt. Integer euismod, nisl at convallis luctus, sapien arcu facilisis massa, vitae posuere libero neque sed augue. Curabitur non magna vel mi consequat suscipit. Donec interdum, sapien a ullamcorper luctus, justo erat tincidunt nibh, sed feugiat lectus urna vitae nulla. Aliquam erat volutpat. Sed posuere, nisl non dignissim luctus, nunc erat sed.

